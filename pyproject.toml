[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mundo-do-wumpus"
version = "0.1.0"
description = "Mundo do Wumpus com agente lógico autônomo"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "rich>=13.7,<15",
    # Persistent terminal interface (UI-RETRO-TUI-001). Textual 8.x renders
    # through Rich and requires rich>=14.2, which the range above allows.
    "textual>=8.2,<9",
]

[project.optional-dependencies]
dev = [
    "pytest>=8,<10",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
